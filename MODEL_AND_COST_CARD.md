# Model and Cost Card

The frozen routing configuration is in `config/models.yaml`, and dated price assumptions are in `config/pricing.yaml`. The application will use only first-party Google Cloud Gemini endpoints with ADC in Live Mode. Deterministic code—not a model—controls eligibility verdicts, proof replay, ranking, and question policy. Actual measured costs and lifecycle checks will be added with Phase 3 and release evaluation artifacts.

